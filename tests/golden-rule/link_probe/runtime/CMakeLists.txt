# Deliberately target-less, mirroring the real runtime/CMakeLists.txt (F7). Its presence is the
# hermetic proof that anti-vacuity is on the COMBINED set: a per-root implementation would fail this
# fixture's clean case, which is spec §5's "single most likely implementation bug in the task".
