#pragma once
// Task 1.1.2 fixture — a zero-field "tag" component: detected, listed with NO field lines. Self-
// contained (no system headers) so it is an all-OS floor for the macro→annotate→detection mechanism.
#include "aero_reflect.hpp"

struct AERO_COMPONENT Tag {};
