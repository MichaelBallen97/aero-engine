# 01 — Tech Stack

> Reviewed and verified July 2026. The stack was independently assessed layer-by-layer; no major pillar was swapped. Two refinements were made against the original draft (**quickjs-ng** over Bellard's QuickJS; the reflection ADR reframed around the new C++26 reality) and two risks logged (SDL3 dependency concentration; SDL_shadercross still in preview) — see [08 — Risks](./08-risks.md).
>
> A **third pass (July 12–13, 2026)** re-verified every pillar against live releases and commit activity: **zero swaps**. Two cosmetic changes: **imnodes** dropped from the v1 table (nothing in v1 needs a node editor — it returns with the v2 graph editors), **stb_vorbis** added for OGG decode (arrives with epic 3.7). Verified versions in the notes below.

---

## Full table

| Layer | Choice | Discarded alternatives | Reason |
|---|---|---|---|
| **Core language** | **C++20** (→ C++23 where all 3 compilers allow) | Rust, Zig, C | ADR-001 |
| **Build** | CMake + Presets + vcpkg (manifest mode) | Bazel, xmake, Premake | Universal; works with everything else |
| **Platform** | **SDL3** (3.4.x+) | GLFW, sokol_app | GLFW has no mobile. SDL3 covers all 5 runtime platforms |
| **RHI** | **SDL3 GPU API** | bgfx, wgpu-native, Dawn, custom RHI | Abstracts Vulkan/Metal/D3D12 in one API. Zero extra deps. Compute supported. ADR-002 |
| **Shaders** | HLSL → **SDL_shadercross** → DXIL / MSL / SPIR-V | GLSL, Slang, writing 3× | One source language, offline compilation |
| **Math** | Own types in `core/math`, **GLM** backend | GLM exposed, RTM direct | ADR-005. Swap to RTM without touching the engine |
| **ECS** | **EnTT** | flecs, classic scene graph | De-facto C++ standard. `entt::meta` gives the reflection runtime |
| **Reflection** | **Code-gen with libclang** → `entt::meta` | Manual registration, macros, C++26 static reflection | ADR-004. From Phase 1 |
| **Serialization** | JSON (text, git-mergeable) in editor; binary in runtime | Binary only | Generated by reflection |
| **Physics 3D** | **Jolt** | PhysX, Bullet | Modern, deterministic, cross-platform, AAA-proven |
| **Physics 2D** | **Box2D v3** | Chipmunk | SIMD rewrite by Erin Catto. Phase 7 |
| **Audio** | Own abstraction → **miniaudio** backend | FMOD, Wwise, SoLoud, SDL audio | ADR-006. FMOD/Wwise excluded by license |
| **OGG decode** | **stb_vorbis** (arrives with epic 3.7) | libvorbis | miniaudio natively decodes wav/flac/mp3; ogg needs stb_vorbis |
| **Canonical 3D format** | **glTF 2.0** (`fastgltf`) | FBX as canonical, OBJ | ADR-003. Open, PBR-native, with skinning |
| **FBX import** | **ufbx** (MIT, single-file C) | Assimp, FBX SDK | Lighter and more correct than Assimp for FBX |
| **OBJ import** | `tinyobjloader` | — | Static meshes only |
| **`.blend` import** | **Invoke Blender CLI** → glTF | Parse `.blend` by hand | ADR-003. This is what Unity does |
| **Other imports** | Assimp (DAE, PLY, STL…) — **editor only** | — | Never enters the runtime |
| **Textures** | stb_image (import) → **KTX2 / Basis Universal** (runtime) | PNG at runtime | GPU compression: BCn (desktop), ASTC/ETC2 (mobile) |
| **Fonts** | msdf-atlas-gen | FreeType direct, stb_truetype | Crisp text at any scale |
| **Scripting VM** | **quickjs-ng** | Bellard's QuickJS, Luau, C#, custom language | ADR-007. Better-maintained fork, CMake build, Windows support |
| **Scripting language** | **TypeScript** on quickjs-ng, *or* native **C++** (per project) | Combined languages | ADR-007 / ADR-008 |
| **TS → JS** | esbuild or swc | tsc | Speed in the import pipeline |
| **Editor UI** | **Dear ImGui** (docking) + ImGuizmo | Qt, custom retained UI; imnodes (deferred to the v2 graph editors — see [future roadmap](./future-roadmap.md)) | Trivial integration, minimal dependency |
| **Profiling** | **Tracy** | Optick, Remotery | From day 1, not later |
| **Jobs / threading** | enkiTS | Custom, TBB | |
| **Logging** | spdlog | | |
| **Tests** | doctest | Catch2, GoogleTest | Fast compilation |
| **CI** | GitHub Actions (macOS + Windows + Ubuntu) | | From commit #1 |

---

## Verification notes (July 2026)

Grounded against current sources, not memory:

**Third pass — versions verified July 12–13, 2026** (GitHub releases/commits): SDL3 **3.4.12** · SDL_shadercross **3.0.0-preview2** (unchanged since April — R4 stands) · Jolt **5.6.0** · quickjs-ng **0.15.1** · EnTT **3.16.0** · Box2D **3.1.1** · Dear ImGui **1.92.8** (docking) · ImGuizmo (active, July 2026) · **doctest 2.5.3** (July 2026 — maintainership resumed in 2024–25 with a steady release cadence; the "is doctest abandoned?" concern was investigated and is resolved) · miniaudio **0.11.25** · fastgltf **0.9.0** (0.10+ will require C++20 — compatible) · ufbx **0.23.0** · Tracy **0.13.1** · spdlog **1.17.0** · KTX-Software **4.4.2** · enkiTS (active; repo lives at `dougbinks/enkiTS`) · GLM (maintenance mode, as assumed — RTM swap path designed, ADR-005) · tinyobjloader, msdf-atlas-gen (active). Verdict: **keep everything; zero swaps.**

- **SDL3 GPU API** is merged and part of official SDL3; **compute shaders are supported** (`SDL_BeginGPUComputePass`). No ray tracing or mesh shaders — the maintainers have stated these are not near-future items. This ceiling is accepted (see limits below).
- **SDL_shadercross** is still **preview** (v3.0.0-preview2, April 2026), actively maintained; it wraps DirectX Shader Compiler (HLSL→SPIR-V, DXIL) and SPIRV-Cross (→MSL/HLSL). Kept behind `tools/shaderc` so we can fall back to raw DXC + SPIRV-Cross if a preview bug blocks us.
- **quickjs-ng** ships releases roughly every 2 months, 40+ contributors, 400+ PRs, tests across 50+ OS/build/sanitizer configs, and uses a **CMake build** with proper Windows support — decisive over Bellard's original for a 3-OS CMake+vcpkg build with sanitizers in CI.
- **C++26 static reflection (P2996)** was voted into C++26 (Sofia feature freeze, June 2025) but is only partially implemented in Clang/GCC trunk and **not in MSVC** as of 2026. Libclang code-gen remains correct for a cross-compiler build today; a migration path to native `std::meta` is designed in (see [ADR-004](./02-adrs.md#adr-004--reflection-by-code-generation)).

---

## Licenses — a hard requirement

The project is **MIT**. Every linked dependency must be permissive-compatible.

| Dependency | License | Links into runtime? |
|---|---|---|
| SDL3 | Zlib | ✅ Yes |
| Jolt | MIT | ✅ Yes |
| EnTT | MIT | ✅ Yes |
| Dear ImGui | MIT | ⚠️ Editor only |
| miniaudio | MIT-0 / public domain | ✅ Yes |
| quickjs-ng | MIT | ✅ Yes |
| fastgltf | MIT | ✅ Yes |
| ufbx | MIT | ⚠️ Editor only |
| tinyobjloader | MIT | ⚠️ Editor only |
| Box2D v3 | MIT | ✅ Yes |
| spdlog | MIT | ✅ Yes |
| GLM | MIT (Happy Bunny) | ✅ Yes |
| stb_image | MIT / public domain | ⚠️ Editor only |
| stb_vorbis | MIT / public domain | ✅ Yes (with epic 3.7) |
| enkiTS | Zlib | ✅ Yes |
| msdf-atlas-gen | MIT | ⚠️ Editor only |
| Assimp | BSD-3 | ⚠️ **Editor only** |
| Tracy | BSD-3 | ⚠️ Dev builds only |
| KTX / Basis Universal | Apache-2.0 | ✅ Yes |
| libclang / LLVM | Apache-2.0 + LLVM exception | 🔧 Build-time only (`reflect-gen`) |
| esbuild / swc | MIT / Apache-2.0 | 🔧 Build-time only |
| Blender | GPL | 🔧 **Invoked as an external process only** — not linked, does not contaminate |

### Excluded by license

- **FMOD** and **Wwise** — proprietary. Cannot be distributed in an open-source project. They can be supported later as **third-party plugins** the user integrates themselves — exactly what Godot does.
- **Autodesk FBX SDK** — restrictive license. This is why `ufbx` is used.

### Note on Blender and the GPL

Blender is GPL, but it is **invoked as an external process** (`blender --background ...`), not linked. That does not contaminate the engine's license. It is the same model Unity uses.

---

## Accepted stack limits

Doors consciously closed. Knowing them now avoids frustration later.

| Limit | Consequence |
|---|---|
| **SDL_GPU has no ray tracing or mesh shaders** | No RT, no Nanite in v1. If ever needed (v3–v4), drop to raw Vulkan/Metal/D3D12 *behind the `rhi` wrapper* |
| **quickjs-ng has no JIT** | Slower than LuaJIT in pure compute. The price of iOS working (iOS forbids JIT) |
| **glTF as canonical** | Exotic formats need an importer written. Not a problem — it is the architecture |
| **Dear ImGui** | The editor looks "gamedev," not "native app." Acceptable |
| **The editor does not run on mobile** | Ever. And that is fine |
